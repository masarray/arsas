<Window x:Class="ArIED61850Tester.RcbExportFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RCB Export Filter — Legacy SAS"
        Height="610" Width="1000" MinHeight="560" MinWidth="880"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{StaticResource AppBackgroundGradient}"
        FontFamily="Aptos, Segoe UI Variable Text, Segoe UI, Calibri"
        Icon="Assets/app-icon.ico" Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="RcbInk" Color="#20304A"/>
        <SolidColorBrush x:Key="RcbMuted" Color="#68788E"/>

        <Style x:Key="RcbTableHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="12,0,8,0"/>
            <Setter Property="Background" Value="#EDF3FA"/>
            <Setter Property="Foreground" Value="#52647D"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#D7E2F0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="RcbDataGridCell" TargetType="DataGridCell">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderBrush" Value="#E1E8F1"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource RcbInk}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E7F0FD"/>
                    <Setter Property="Foreground" Value="{StaticResource RcbInk}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RcbDataGridRow" TargetType="DataGridRow">
            <Setter Property="Height" Value="44"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1"><Setter Property="Background" Value="#FAFCFF"/></Trigger>
                <DataTrigger Binding="{Binding IsSelectable}" Value="False"><Setter Property="Opacity" Value="0.62"/></DataTrigger>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F0F5FC"/></Trigger>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#E2ECFA"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RcbCellText" TargetType="TextBlock">
            <Setter Property="Margin" Value="12,0,8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>

        <Style x:Key="RcbCheckBox" TargetType="CheckBox">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#D6E1EF" BorderThickness="1"
                CornerRadius="12" Padding="14,9" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding IedName}" FontSize="13.5" FontWeight="SemiBold"
                               Foreground="{StaticResource RcbInk}" VerticalAlignment="Center"/>
                    <TextBlock Text="·" Margin="8,0" FontSize="12" Foreground="#A0ACBC" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Endpoint}" FontSize="11.5" Foreground="{StaticResource RcbMuted}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="7" Height="7" Fill="#16A34A" Margin="0,0,7,0"/>
                    <TextBlock Text="{Binding AvailabilityCheckedText}" FontSize="11.5"
                               Foreground="{StaticResource RcbInk}" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <Button Grid.Column="4" x:Name="CheckAvailabilityButton" Style="{StaticResource SoftButton}"
                        Padding="12,7" Click="CheckAvailability_Click" ToolTip="Read-only availability check">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M18,8 A7,7 0 1 0 19,13 M18,3 V8 H13" Width="14" Height="14"
                              Stretch="Uniform" Stroke="#315DBF" StrokeThickness="1.8" Fill="Transparent"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round" Margin="0,0,6,0"/>
                        <TextBlock x:Name="CheckAvailabilityText" Text="Check Availability"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#D4E0EF" BorderThickness="1" CornerRadius="0" ClipToBounds="True">
            <DataGrid x:Name="RcbGrid" ItemsSource="{Binding Rows}" SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                      CanUserSortColumns="False" HeadersVisibility="Column" GridLinesVisibility="All"
                      HorizontalGridLinesBrush="#E1E8F1" VerticalGridLinesBrush="#E1E8F1"
                      RowHeaderWidth="0" RowHeight="44" AlternationCount="2"
                      SelectionMode="Single" SelectionUnit="FullRow" VerticalContentAlignment="Center"
                      Background="Transparent" BorderThickness="0"
                      ColumnHeaderStyle="{StaticResource RcbTableHeader}"
                      CellStyle="{StaticResource RcbDataGridCell}" RowStyle="{StaticResource RcbDataGridRow}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="SELECT" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Stretch">
                                    <CheckBox Style="{StaticResource RcbCheckBox}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsSelectable}"
                                              Checked="RcbCheckBox_Checked" Unchecked="RcbCheckBox_Unchecked"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="REPORT CONTROL BLOCK" Width="2.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource RcbCellText}" Text="{Binding Name}"
                                           FontWeight="SemiBold" Foreground="{StaticResource RcbInk}"
                                           ToolTip="{Binding Reference}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="TYPE" Width="1.05*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource RcbCellText}" Text="{Binding Type}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="DATASET" Width="2.15*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource RcbCellText}" Text="{Binding DataSetName}"
                                           FontWeight="SemiBold" Foreground="{StaticResource RcbInk}"
                                           ToolTip="{Binding DataSetDetail}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="MEMBERS" Width="98">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource RcbCellText}" Text="{Binding MemberCountText}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="STATUS" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="12,0,8,0"
                                            VerticalAlignment="Center" ToolTip="{Binding Reason}">
                                    <Ellipse Width="7" Height="7" Fill="{Binding StatusBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding StatusText}" Margin="7,0,0,0" FontWeight="SemiBold"
                                               Foreground="{Binding StatusBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="4" ToolTip="{Binding SafetyText}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding SelectionSummary}" FontSize="11.8" FontWeight="SemiBold"
                           Foreground="{StaticResource RcbInk}" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding RemovalSummary}" Margin="12,0,0,0" FontSize="11.2"
                           Foreground="#315DBF" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="MockStatusText" Visibility="Collapsed"
                       Text="Select one available RCB. Availability checks are read-only."/>
            <TextBlock Visibility="Collapsed" Text="Original file remains unchanged"/>

            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource SoftButton}" IsCancel="True" MinWidth="96" Margin="0,0,10,0">
                    <TextBlock Text="Cancel"/>
                </Button>
                <Button x:Name="ExportButton" Style="{StaticResource PrimaryButton}" MinWidth="132"
                        Click="Export_Click" IsDefault="True">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M5,3 H15 L19,7 V21 H5 Z M15,3 V8 H19 M9,13 H15 M9,17 H15"
                              Width="15" Height="15" Stretch="Uniform" Stroke="White" StrokeThickness="1.6"
                              Fill="Transparent" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Margin="0,0,7,0"/>
                        <TextBlock Text="Export RCB"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>