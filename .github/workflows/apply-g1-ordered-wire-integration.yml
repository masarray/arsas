name: Apply G1 final ordered wire integration

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'fix/g1-control-correctness'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply final ordered wire integration
        run: python scripts/apply-g1-ordered-wire-integration.py
      - name: Remove one-shot staging files
        run: |
          rm scripts/apply-g1-ordered-wire-integration.py
          rm .github/workflows/apply-g1-ordered-wire-integration.yml
      - name: Commit materialized source
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'feat(control): surface ordered SBO and Oper wire evidence'
          git push origin HEAD:${{ github.head_ref }}
