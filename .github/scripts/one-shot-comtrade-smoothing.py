from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]


def read(path):
    return (ROOT / path).read_text(encoding="utf-8")


def write(path, text):
    (ROOT / path).write_text(text, encoding="utf-8", newline="\n")


def replace_once(path, old, new):
    text = read(path)
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{path}: expected exactly one match, found {count}: {old[:100]!r}")
    write(path, text.replace(old, new, 1))


def replace_between(path, start, end, replacement):
    text = read(path)
    a = text.find(start)
    if a < 0:
        raise SystemExit(f"{path}: start marker not found: {start!r}")
    b = text.find(end, a)
    if b < 0:
        raise SystemExit(f"{path}: end marker not found: {end!r}")
    write(path, text[:a] + replacement + text[b:])


# Shared time-based presentation math: ease visually, snap deterministically near target.
replace_once(
    "Services/PresentationEasingMath.cs",
    "    public static double SmoothAngleDegrees(double currentDegrees, double targetDegrees, double elapsedMilliseconds, double timeConstantMilliseconds)\n    {\n        if (!double.IsFinite(targetDegrees)) return NormalizeAngleDegrees(currentDegrees);\n        if (!double.IsFinite(currentDegrees)) return NormalizeAngleDegrees(targetDegrees);\n        var alpha = ExponentialAlpha(elapsedMilliseconds, timeConstantMilliseconds);\n        return NormalizeAngleDegrees(currentDegrees + (ShortestAngleDeltaDegrees(currentDegrees, targetDegrees) * alpha));\n    }\n",
    "    public static double SmoothAngleDegrees(double currentDegrees, double targetDegrees, double elapsedMilliseconds, double timeConstantMilliseconds)\n    {\n        if (!double.IsFinite(targetDegrees)) return NormalizeAngleDegrees(currentDegrees);\n        if (!double.IsFinite(currentDegrees)) return NormalizeAngleDegrees(targetDegrees);\n        var alpha = ExponentialAlpha(elapsedMilliseconds, timeConstantMilliseconds);\n        return NormalizeAngleDegrees(currentDegrees + (ShortestAngleDeltaDegrees(currentDegrees, targetDegrees) * alpha));\n    }\n\n    public static bool IsSettled(double current, double target, double absoluteTolerance, double relativeTolerance = 0.0)\n    {\n        if (!double.IsFinite(current) || !double.IsFinite(target)) return current.Equals(target);\n        var tolerance = Math.Max(Math.Max(0.0, absoluteTolerance), Math.Abs(target) * Math.Max(0.0, relativeTolerance));\n        return Math.Abs(target - current) <= tolerance;\n    }\n\n    public static double SmoothAndSnap(\n        double current,\n        double target,\n        double elapsedMilliseconds,\n        double timeConstantMilliseconds,\n        double absoluteTolerance,\n        double relativeTolerance = 0.0)\n    {\n        var next = Smooth(current, target, elapsedMilliseconds, timeConstantMilliseconds);\n        return IsSettled(next, target, absoluteTolerance, relativeTolerance) ? target : next;\n    }\n\n    public static double SmoothAngleAndSnapDegrees(\n        double currentDegrees,\n        double targetDegrees,\n        double elapsedMilliseconds,\n        double timeConstantMilliseconds,\n        double toleranceDegrees = 0.08)\n    {\n        var next = SmoothAngleDegrees(currentDegrees, targetDegrees, elapsedMilliseconds, timeConstantMilliseconds);\n        return Math.Abs(ShortestAngleDeltaDegrees(next, targetDegrees)) <= Math.Max(0.0, toleranceDegrees)\n            ? NormalizeAngleDegrees(targetDegrees)\n            : next;\n    }\n")

# TIME workspace: cache static tracks/grid and ease only cursor overlay at composition cadence.
replace_once("Controls/ComtradeDisturbanceView.cs", "using System.Globalization;\n", "using System.Diagnostics;\nusing System.Globalization;\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "    private double? _cursor1Milliseconds;\n    private double? _cursor2Milliseconds;\n    private Rect _lastPlot;\n",
    "    private double? _cursor1Milliseconds;\n    private double? _cursor2Milliseconds;\n    private double? _presentedCursor1Milliseconds;\n    private double? _presentedCursor2Milliseconds;\n    private bool _cursorPresentationRenderingHooked;\n    private long _cursorPresentationTimestamp;\n    private DrawingGroup? _staticLayer;\n    private double _staticLayerWidth = double.NaN;\n    private double _staticLayerHeight = double.NaN;\n    private Rect _lastTimelinePlot;\n    private const double CursorPresentationTimeConstantMs = 38.0;\n    private Rect _lastPlot;\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        ToolTip = \"Wheel: scroll signals • Ctrl+wheel: zoom • Drag plot: pan • Drag C1/C2: move • Right-click: C2 • cursors snap to digital edges\";\n",
    "        ToolTip = \"Wheel: scroll signals • Ctrl+wheel: zoom • Drag plot: pan • Drag C1/C2: move • Right-click: C2 • cursors snap to digital edges\";\n        Unloaded += (_, _) => StopCursorPresentationPump();\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        if (!preserveCursor)\n        {\n            _cursor1Milliseconds = null;\n            _cursor2Milliseconds = null;\n        }\n\n        Height = Math.Max(330, TopMargin + BottomAxisHeight + _tracks.Sum(track => TrackHeight(track) + TrackGap));\n        InvalidateVisual();\n",
    "        if (!preserveCursor)\n        {\n            _cursor1Milliseconds = null;\n            _cursor2Milliseconds = null;\n            _presentedCursor1Milliseconds = null;\n            _presentedCursor2Milliseconds = null;\n        }\n\n        Height = Math.Max(330, TopMargin + BottomAxisHeight + _tracks.Sum(track => TrackHeight(track) + TrackGap));\n        InvalidateStaticLayer();\n        InvalidateVisual();\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        _cursor1Milliseconds = null;\n        _cursor2Milliseconds = null;\n        Height = 330;\n        ToolTip = message;\n        InvalidateVisual();\n",
    "        _cursor1Milliseconds = null;\n        _cursor2Milliseconds = null;\n        _presentedCursor1Milliseconds = null;\n        _presentedCursor2Milliseconds = null;\n        StopCursorPresentationPump();\n        Height = 330;\n        ToolTip = message;\n        InvalidateStaticLayer();\n        InvalidateVisual();\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        _cursor1Milliseconds = cursors.Cursor1Milliseconds;\n        _cursor2Milliseconds = cursors.Cursor2Milliseconds;\n        InvalidateVisual();\n",
    "        _cursor1Milliseconds = cursors.Cursor1Milliseconds;\n        _cursor2Milliseconds = cursors.Cursor2Milliseconds;\n        _presentedCursor1Milliseconds = _cursor1Milliseconds;\n        _presentedCursor2Milliseconds = _cursor2Milliseconds;\n        StopCursorPresentationPump();\n        InvalidateVisual();\n")
replace_between(
    "Controls/ComtradeDisturbanceView.cs",
    "    internal void SetCursorFromHost(ComtradeDisturbanceCursor cursor, double milliseconds)\n",
    "    internal void ResetNavigation()\n",
    "    internal void SetCursorFromHost(ComtradeDisturbanceCursor cursor, double milliseconds)\n    {\n        if (!double.IsFinite(milliseconds))\n            return;\n        if (cursor == ComtradeDisturbanceCursor.Cursor1)\n            _cursor1Milliseconds = milliseconds;\n        else\n            _cursor2Milliseconds = milliseconds;\n        EnsureCursorPresentationPump();\n        RaiseNavigationChanged();\n    }\n\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        _viewStartMilliseconds = _fullStartMilliseconds;\n        _viewEndMilliseconds = _fullEndMilliseconds;\n        InvalidateVisual();\n",
    "        _viewStartMilliseconds = _fullStartMilliseconds;\n        _viewEndMilliseconds = _fullEndMilliseconds;\n        InvalidateStaticLayer();\n        InvalidateVisual();\n")

new_render = '''    protected override void OnRender(DrawingContext dc)\n    {\n        base.OnRender(dc);\n        var bounds = new Rect(0, 0, Math.Max(0, ActualWidth), Math.Max(0, ActualHeight));\n        dc.DrawRectangle(Brushes.White, null, bounds);\n        if (bounds.Width < 320 || bounds.Height < 160)\n            return;\n\n        EnsureStaticLayer(bounds);\n        if (_staticLayer is not null)\n            dc.DrawDrawing(_staticLayer);\n        if (_tracks.Count == 0 || !_lastTimelinePlot.IsEmpty)\n        {\n            var dpi = VisualTreeHelper.GetDpi(this).PixelsPerDip;\n            var semibold = new Typeface("Segoe UI Semibold");\n            DrawCursor(dc, _lastTimelinePlot, _presentedCursor1Milliseconds ?? _cursor1Milliseconds, "C1", Color.FromRgb(221, 142, 32), dpi, semibold);\n            DrawCursor(dc, _lastTimelinePlot, _presentedCursor2Milliseconds ?? _cursor2Milliseconds, "C2", Color.FromRgb(36, 172, 211), dpi, semibold);\n        }\n    }\n\n    private void EnsureStaticLayer(Rect bounds)\n    {\n        if (_staticLayer is not null &&\n            Math.Abs(_staticLayerWidth - bounds.Width) <= 0.25 &&\n            Math.Abs(_staticLayerHeight - bounds.Height) <= 0.25)\n            return;\n\n        _staticLayerWidth = bounds.Width;\n        _staticLayerHeight = bounds.Height;\n        var group = new DrawingGroup();\n        using (var dc = group.Open())\n            DrawStaticContent(dc, bounds);\n        group.Freeze();\n        _staticLayer = group;\n    }\n\n    private void DrawStaticContent(DrawingContext dc, Rect bounds)\n    {\n        var dpi = VisualTreeHelper.GetDpi(this).PixelsPerDip;\n        var body = new Typeface("Segoe UI");\n        var semibold = new Typeface("Segoe UI Semibold");\n        var plotWidth = Math.Max(80, bounds.Width - LabelWidth - RightMargin);\n        _lastPlot = new Rect(LabelWidth, TopMargin, plotWidth, Math.Max(80, bounds.Height - TopMargin - BottomAxisHeight));\n        _lastTimelinePlot = Rect.Empty;\n\n        if (_tracks.Count == 0 || _viewEndMilliseconds <= _viewStartMilliseconds)\n        {\n            DrawText(dc, "Select signals from the left panel to build a synchronized disturbance timeline.", 12,\n                body, Color.FromRgb(119, 133, 151), new Point(LabelWidth + 18, 42), dpi);\n            return;\n        }\n\n        var y = TopMargin;\n        foreach (var track in _tracks)\n        {\n            var height = TrackHeight(track);\n            var row = new Rect(0, y, bounds.Width, height);\n            var plot = new Rect(LabelWidth, y, plotWidth, height);\n            DrawTrackBackground(dc, row, plot);\n            DrawTrackLabel(dc, track, row, dpi, body, semibold);\n            if (track.IsDigital)\n                DrawDigitalTrack(dc, track, plot, dpi, body);\n            else\n                DrawAnalogTrack(dc, track, plot, dpi, body);\n            y += height + TrackGap;\n        }\n\n        var tracksBottom = Math.Min(bounds.Height - BottomAxisHeight, y - TrackGap);\n        _lastTimelinePlot = new Rect(LabelWidth, TopMargin, plotWidth, Math.Max(1, tracksBottom - TopMargin));\n        DrawTrigger(dc, _lastTimelinePlot, dpi, semibold);\n        DrawTimeAxis(dc, new Rect(LabelWidth, tracksBottom, plotWidth, BottomAxisHeight), dpi, body, semibold);\n    }\n\n    private void InvalidateStaticLayer()\n    {\n        _staticLayer = null;\n        _staticLayerWidth = double.NaN;\n        _staticLayerHeight = double.NaN;\n    }\n\n'''
replace_between(
    "Controls/ComtradeDisturbanceView.cs",
    "    protected override void OnRender(DrawingContext dc)\n",
    "    protected override void OnMouseWheel(MouseWheelEventArgs e)\n",
    new_render)

replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        if (cursor == ComtradeDisturbanceCursor.Cursor1)\n            _cursor1Milliseconds = value;\n        else\n            _cursor2Milliseconds = value;\n\n        InvalidateVisual();\n        RaiseNavigationChanged();\n",
    "        if (cursor == ComtradeDisturbanceCursor.Cursor1)\n            _cursor1Milliseconds = value;\n        else\n            _cursor2Milliseconds = value;\n\n        EnsureCursorPresentationPump();\n        RaiseNavigationChanged();\n")

cursor_pump = '''    private void EnsureCursorPresentationPump()\n    {\n        if (_presentedCursor1Milliseconds is null && _cursor1Milliseconds is not null)\n            _presentedCursor1Milliseconds = _cursor1Milliseconds;\n        if (_presentedCursor2Milliseconds is null && _cursor2Milliseconds is not null)\n            _presentedCursor2Milliseconds = _cursor2Milliseconds;\n        if (_cursorPresentationRenderingHooked) return;\n        _cursorPresentationTimestamp = Stopwatch.GetTimestamp();\n        CompositionTarget.Rendering += CursorPresentationFrame;\n        _cursorPresentationRenderingHooked = true;\n    }\n\n    private void StopCursorPresentationPump()\n    {\n        if (_cursorPresentationRenderingHooked)\n            CompositionTarget.Rendering -= CursorPresentationFrame;\n        _cursorPresentationRenderingHooked = false;\n        _cursorPresentationTimestamp = 0;\n    }\n\n    private void CursorPresentationFrame(object? sender, EventArgs e)\n    {\n        var now = Stopwatch.GetTimestamp();\n        var elapsedMilliseconds = _cursorPresentationTimestamp == 0\n            ? 16.67\n            : Math.Clamp(Stopwatch.GetElapsedTime(_cursorPresentationTimestamp, now).TotalMilliseconds, 1.0, 50.0);\n        _cursorPresentationTimestamp = now;\n\n        var moving1 = AdvancePresentedCursor(ref _presentedCursor1Milliseconds, _cursor1Milliseconds, elapsedMilliseconds);\n        var moving2 = AdvancePresentedCursor(ref _presentedCursor2Milliseconds, _cursor2Milliseconds, elapsedMilliseconds);\n        InvalidateVisual();\n        if (!moving1 && !moving2)\n            StopCursorPresentationPump();\n    }\n\n    private bool AdvancePresentedCursor(ref double? presented, double? target, double elapsedMilliseconds)\n    {\n        if (target is null)\n        {\n            presented = null;\n            return false;\n        }\n        if (presented is null || !double.IsFinite(presented.Value))\n        {\n            presented = target;\n            return false;\n        }\n\n        var snapTolerance = Math.Max(0.0005,\n            Math.Max(0.001, _viewEndMilliseconds - _viewStartMilliseconds) / Math.Max(1.0, _lastPlot.Width) * 0.12);\n        presented = PresentationEasingMath.SmoothAndSnap(\n            presented.Value, target.Value, elapsedMilliseconds, CursorPresentationTimeConstantMs, snapTolerance);\n        return !PresentationEasingMath.IsSettled(presented.Value, target.Value, snapTolerance);\n    }\n\n'''
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "    private bool TrySnapToVisibleDigitalEdge(double requestedMilliseconds, double toleranceMilliseconds, out double snappedMilliseconds)\n",
    cursor_pump + "    private bool TrySnapToVisibleDigitalEdge(double requestedMilliseconds, double toleranceMilliseconds, out double snappedMilliseconds)\n")
replace_once(
    "Controls/ComtradeDisturbanceView.cs",
    "        _viewStartMilliseconds = start;\n        _viewEndMilliseconds = start + span;\n",
    "        _viewStartMilliseconds = start;\n        _viewEndMilliseconds = start + span;\n        InvalidateStaticLayer();\n")

# PHASOR workspace: continuously ease toward the newest native frame at composition cadence.
replace_once(
    "Controls/ComtradePhasorView.cs",
    "    private const double PresentationTimeConstantMs = 78.0;\n",
    "    private const double PresentationTimeConstantMs = 54.0;\n")
replace_once(
    "Controls/ComtradePhasorView.cs",
    "    private ComtradePhasorVector[] _smoothedVoltageVectors = Array.Empty<ComtradePhasorVector>();\n    private ComtradePhasorVector[] _smoothedCurrentVectors = Array.Empty<ComtradePhasorVector>();\n    private long _lastPresentationTimestamp;\n",
    "    private ComtradePhasorVector[] _smoothedVoltageVectors = Array.Empty<ComtradePhasorVector>();\n    private ComtradePhasorVector[] _smoothedCurrentVectors = Array.Empty<ComtradePhasorVector>();\n    private ComtradePhasorVector[] _targetVoltageVectors = Array.Empty<ComtradePhasorVector>();\n    private ComtradePhasorVector[] _targetCurrentVectors = Array.Empty<ComtradePhasorVector>();\n    private bool _presentationRenderingHooked;\n    private long _lastPresentationTimestamp;\n\n    public ComtradePhasorView()\n    {\n        Unloaded += (_, _) => StopPresentationPump();\n    }\n")
replace_between(
    "Controls/ComtradePhasorView.cs",
    "    internal void ShowPhasors(\n",
    "    internal void ShowMessage(string title, string message)\n",
    '''    internal void ShowPhasors(\n        string referenceLabel,\n        string referenceDetail,\n        IReadOnlyList<ComtradePhasorVector> voltageVectors,\n        IReadOnlyList<ComtradePhasorVector> currentVectors)\n    {\n        var resolvedReference = string.IsNullOrWhiteSpace(referenceLabel) ? "Reference" : referenceLabel;\n        _headerLabel = $"Fundamental phasors at {resolvedReference}";\n        _referenceDetail = referenceDetail ?? string.Empty;\n        _targetVoltageVectors = voltageVectors?.ToArray() ?? Array.Empty<ComtradePhasorVector>();\n        _targetCurrentVectors = currentVectors?.ToArray() ?? Array.Empty<ComtradePhasorVector>();\n\n        if (!VectorTopologyMatches(_smoothedVoltageVectors, _targetVoltageVectors))\n            _smoothedVoltageVectors = _targetVoltageVectors.ToArray();\n        if (!VectorTopologyMatches(_smoothedCurrentVectors, _targetCurrentVectors))\n            _smoothedCurrentVectors = _targetCurrentVectors.ToArray();\n\n        _voltagePanel = PreparePanel(_smoothedVoltageVectors);\n        _currentPanel = PreparePanel(_smoothedCurrentVectors);\n        _message = string.Empty;\n        if (VectorsSettled(_smoothedVoltageVectors, _targetVoltageVectors) &&\n            VectorsSettled(_smoothedCurrentVectors, _targetCurrentVectors))\n            StopPresentationPump();\n        else\n            EnsurePresentationPump();\n        InvalidateVisual();\n    }\n\n''')
replace_once(
    "Controls/ComtradePhasorView.cs",
    "        _smoothedVoltageVectors = Array.Empty<ComtradePhasorVector>();\n        _smoothedCurrentVectors = Array.Empty<ComtradePhasorVector>();\n        _lastPresentationTimestamp = 0;\n",
    "        _smoothedVoltageVectors = Array.Empty<ComtradePhasorVector>();\n        _smoothedCurrentVectors = Array.Empty<ComtradePhasorVector>();\n        _targetVoltageVectors = Array.Empty<ComtradePhasorVector>();\n        _targetCurrentVectors = Array.Empty<ComtradePhasorVector>();\n        StopPresentationPump();\n")
phasor_pump = '''    private void EnsurePresentationPump()\n    {\n        if (_presentationRenderingHooked) return;\n        _lastPresentationTimestamp = Stopwatch.GetTimestamp();\n        CompositionTarget.Rendering += PresentationFrame;\n        _presentationRenderingHooked = true;\n    }\n\n    private void StopPresentationPump()\n    {\n        if (_presentationRenderingHooked)\n            CompositionTarget.Rendering -= PresentationFrame;\n        _presentationRenderingHooked = false;\n        _lastPresentationTimestamp = 0;\n    }\n\n    private void PresentationFrame(object? sender, EventArgs e)\n    {\n        var now = Stopwatch.GetTimestamp();\n        var elapsedMilliseconds = _lastPresentationTimestamp == 0\n            ? 16.67\n            : Math.Clamp(Stopwatch.GetElapsedTime(_lastPresentationTimestamp, now).TotalMilliseconds, 1.0, 50.0);\n        _lastPresentationTimestamp = now;\n\n        _smoothedVoltageVectors = SmoothVectors(_smoothedVoltageVectors, _targetVoltageVectors, elapsedMilliseconds);\n        _smoothedCurrentVectors = SmoothVectors(_smoothedCurrentVectors, _targetCurrentVectors, elapsedMilliseconds);\n        var settled = VectorsSettled(_smoothedVoltageVectors, _targetVoltageVectors) &&\n                      VectorsSettled(_smoothedCurrentVectors, _targetCurrentVectors);\n        if (settled)\n        {\n            _smoothedVoltageVectors = _targetVoltageVectors.ToArray();\n            _smoothedCurrentVectors = _targetCurrentVectors.ToArray();\n        }\n        _voltagePanel = PreparePanel(_smoothedVoltageVectors);\n        _currentPanel = PreparePanel(_smoothedCurrentVectors);\n        InvalidateVisual();\n        if (settled) StopPresentationPump();\n    }\n\n    private static bool VectorTopologyMatches(IReadOnlyList<ComtradePhasorVector> left, IReadOnlyList<ComtradePhasorVector> right)\n    {\n        if (left.Count != right.Count || left.Count == 0) return left.Count == 0 && right.Count == 0;\n        for (var index = 0; index < left.Count; index++)\n        {\n            if (!string.Equals(left[index].Label, right[index].Label, StringComparison.Ordinal) ||\n                !string.Equals(left[index].Phase, right[index].Phase, StringComparison.Ordinal) ||\n                !string.Equals(left[index].Units, right[index].Units, StringComparison.Ordinal))\n                return false;\n        }\n        return true;\n    }\n\n    private static bool VectorsSettled(IReadOnlyList<ComtradePhasorVector> current, IReadOnlyList<ComtradePhasorVector> target)\n    {\n        if (!VectorTopologyMatches(current, target)) return false;\n        for (var index = 0; index < target.Count; index++)\n        {\n            if (!PresentationEasingMath.IsSettled(current[index].MagnitudeRms, target[index].MagnitudeRms, 1e-7, 5e-4) ||\n                Math.Abs(PresentationEasingMath.ShortestAngleDeltaDegrees(current[index].AngleDegrees, target[index].AngleDegrees)) > 0.08)\n                return false;\n        }\n        return true;\n    }\n\n'''
replace_once(
    "Controls/ComtradePhasorView.cs",
    "    protected override void OnRender(DrawingContext dc)\n",
    phasor_pump + "    protected override void OnRender(DrawingContext dc)\n")
replace_once(
    "Controls/ComtradePhasorView.cs",
    "                PresentationEasingMath.Smooth(before.MagnitudeRms, next.MagnitudeRms, elapsedMilliseconds, PresentationTimeConstantMs),\n                PresentationEasingMath.SmoothAngleDegrees(before.AngleDegrees, next.AngleDegrees, elapsedMilliseconds, PresentationTimeConstantMs));\n",
    "                PresentationEasingMath.SmoothAndSnap(before.MagnitudeRms, next.MagnitudeRms, elapsedMilliseconds, PresentationTimeConstantMs, 1e-7, 5e-4),\n                PresentationEasingMath.SmoothAngleAndSnapDegrees(before.AngleDegrees, next.AngleDegrees, elapsedMilliseconds, PresentationTimeConstantMs));\n")

# HARMONICS workspace: same continuous latest-target easing; no analysis backlog is introduced.
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "    private const double PresentationTimeConstantMs = 92.0;\n",
    "    private const double PresentationTimeConstantMs = 62.0;\n")
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "    private ComtradeHarmonicOverviewSpectrum[] _smoothedSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n    private long _lastPresentationTimestamp;\n",
    "    private ComtradeHarmonicOverviewSpectrum[] _smoothedSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n    private ComtradeHarmonicOverviewSpectrum[] _targetSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n    private bool _presentationRenderingHooked;\n    private long _lastPresentationTimestamp;\n")
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "        ToolTip = \"Click any harmonic order to compare the same order across all visible analog channels.\";\n",
    "        ToolTip = \"Click any harmonic order to compare the same order across all visible analog channels.\";\n        Unloaded += (_, _) => StopPresentationPump();\n")
replace_between(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "    internal void ShowSpectra(\n",
    "    internal void ShowMessage(string title, string message)\n",
    '''    internal void ShowSpectra(\n        string title,\n        string subtitle,\n        IReadOnlyList<ComtradeHarmonicOverviewSpectrum> spectra)\n    {\n        _title = title ?? string.Empty;\n        _subtitle = subtitle ?? string.Empty;\n        var incoming = spectra ?? Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n        _targetSpectra = incoming.Select(item => item with { Bins = item.Bins.ToArray() }).ToArray();\n        if (!SpectraTopologyMatches(_smoothedSpectra, _targetSpectra))\n            _smoothedSpectra = _targetSpectra.Select(item => item with { Bins = item.Bins.ToArray() }).ToArray();\n\n        _spectra = _smoothedSpectra;\n        _maximumDisplayedOrder = ResolveMaximumDisplayedOrder(_spectra);\n        _selectedOrder = Math.Clamp(_selectedOrder, 0, Math.Max(0, _maximumDisplayedOrder));\n        _preparedRows = PrepareRows(_spectra, _maximumDisplayedOrder);\n        _rowTargets.Clear();\n        if (SpectraSettled(_smoothedSpectra, _targetSpectra))\n            StopPresentationPump();\n        else\n            EnsurePresentationPump();\n        InvalidateVisual();\n    }\n\n''')
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "        _smoothedSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n        _lastPresentationTimestamp = 0;\n",
    "        _smoothedSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n        _targetSpectra = Array.Empty<ComtradeHarmonicOverviewSpectrum>();\n        StopPresentationPump();\n")
harmonic_pump = '''    private void EnsurePresentationPump()\n    {\n        if (_presentationRenderingHooked) return;\n        _lastPresentationTimestamp = Stopwatch.GetTimestamp();\n        CompositionTarget.Rendering += PresentationFrame;\n        _presentationRenderingHooked = true;\n    }\n\n    private void StopPresentationPump()\n    {\n        if (_presentationRenderingHooked)\n            CompositionTarget.Rendering -= PresentationFrame;\n        _presentationRenderingHooked = false;\n        _lastPresentationTimestamp = 0;\n    }\n\n    private void PresentationFrame(object? sender, EventArgs e)\n    {\n        var now = Stopwatch.GetTimestamp();\n        var elapsedMilliseconds = _lastPresentationTimestamp == 0\n            ? 16.67\n            : Math.Clamp(Stopwatch.GetElapsedTime(_lastPresentationTimestamp, now).TotalMilliseconds, 1.0, 50.0);\n        _lastPresentationTimestamp = now;\n        _smoothedSpectra = SmoothSpectra(_smoothedSpectra, _targetSpectra, elapsedMilliseconds);\n        var settled = SpectraSettled(_smoothedSpectra, _targetSpectra);\n        if (settled)\n            _smoothedSpectra = _targetSpectra.Select(item => item with { Bins = item.Bins.ToArray() }).ToArray();\n        _spectra = _smoothedSpectra;\n        _maximumDisplayedOrder = ResolveMaximumDisplayedOrder(_spectra);\n        _selectedOrder = Math.Clamp(_selectedOrder, 0, Math.Max(0, _maximumDisplayedOrder));\n        _preparedRows = PrepareRows(_spectra, _maximumDisplayedOrder);\n        _rowTargets.Clear();\n        InvalidateVisual();\n        if (settled) StopPresentationPump();\n    }\n\n    private static bool SpectraTopologyMatches(IReadOnlyList<ComtradeHarmonicOverviewSpectrum> left, IReadOnlyList<ComtradeHarmonicOverviewSpectrum> right)\n    {\n        if (left.Count != right.Count || left.Count == 0) return left.Count == 0 && right.Count == 0;\n        for (var spectrumIndex = 0; spectrumIndex < left.Count; spectrumIndex++)\n        {\n            if (!string.Equals(left[spectrumIndex].SignalName, right[spectrumIndex].SignalName, StringComparison.Ordinal) ||\n                !string.Equals(left[spectrumIndex].Units, right[spectrumIndex].Units, StringComparison.Ordinal) ||\n                left[spectrumIndex].Bins.Count != right[spectrumIndex].Bins.Count) return false;\n            for (var binIndex = 0; binIndex < left[spectrumIndex].Bins.Count; binIndex++)\n                if (left[spectrumIndex].Bins[binIndex].Order != right[spectrumIndex].Bins[binIndex].Order) return false;\n        }\n        return true;\n    }\n\n    private static bool SpectraSettled(IReadOnlyList<ComtradeHarmonicOverviewSpectrum> current, IReadOnlyList<ComtradeHarmonicOverviewSpectrum> target)\n    {\n        if (!SpectraTopologyMatches(current, target)) return false;\n        for (var spectrumIndex = 0; spectrumIndex < target.Count; spectrumIndex++)\n        {\n            var before = current[spectrumIndex];\n            var next = target[spectrumIndex];\n            if (!PresentationEasingMath.IsSettled(before.DcComponent, next.DcComponent, 1e-8, 8e-4) ||\n                !PresentationEasingMath.IsSettled(before.FundamentalRms, next.FundamentalRms, 1e-8, 8e-4) ||\n                !PresentationEasingMath.IsSettled(before.ThdPercent, next.ThdPercent, 1e-5, 8e-4)) return false;\n            for (var binIndex = 0; binIndex < next.Bins.Count; binIndex++)\n            {\n                var oldBin = before.Bins[binIndex];\n                var newBin = next.Bins[binIndex];\n                if (!PresentationEasingMath.IsSettled(oldBin.MagnitudeRms, newBin.MagnitudeRms, 1e-8, 8e-4) ||\n                    !PresentationEasingMath.IsSettled(oldBin.PercentOfFundamental, newBin.PercentOfFundamental, 1e-5, 8e-4) ||\n                    Math.Abs(PresentationEasingMath.ShortestAngleDeltaDegrees(oldBin.AngleDegrees, newBin.AngleDegrees)) > 0.08)\n                    return false;\n            }\n        }\n        return true;\n    }\n\n'''
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "    protected override void OnRender(DrawingContext dc)\n",
    harmonic_pump + "    protected override void OnRender(DrawingContext dc)\n")
replace_once(
    "Controls/ComtradeHarmonicsWorkstationView.cs",
    "                    PresentationEasingMath.Smooth(previousBin.MagnitudeRms, targetBin.MagnitudeRms, elapsedMilliseconds, PresentationTimeConstantMs),\n                    PresentationEasingMath.Smooth(previousBin.PercentOfFundamental, targetBin.PercentOfFundamental, elapsedMilliseconds, PresentationTimeConstantMs),\n                    PresentationEasingMath.SmoothAngleDegrees(previousBin.AngleDegrees, targetBin.AngleDegrees, elapsedMilliseconds, PresentationTimeConstantMs));\n",
    "                    PresentationEasingMath.SmoothAndSnap(previousBin.MagnitudeRms, targetBin.MagnitudeRms, elapsedMilliseconds, PresentationTimeConstantMs, 1e-8, 8e-4),\n                    PresentationEasingMath.SmoothAndSnap(previousBin.PercentOfFundamental, targetBin.PercentOfFundamental, elapsedMilliseconds, PresentationTimeConstantMs, 1e-5, 8e-4),\n                    PresentationEasingMath.SmoothAngleAndSnapDegrees(previousBin.AngleDegrees, targetBin.AngleDegrees, elapsedMilliseconds, PresentationTimeConstantMs));\n")
for old, new in [
    ("PresentationEasingMath.Smooth(before.DcComponent, next.DcComponent, elapsedMilliseconds, PresentationTimeConstantMs)", "PresentationEasingMath.SmoothAndSnap(before.DcComponent, next.DcComponent, elapsedMilliseconds, PresentationTimeConstantMs, 1e-8, 8e-4)"),
    ("PresentationEasingMath.Smooth(before.FundamentalRms, next.FundamentalRms, elapsedMilliseconds, PresentationTimeConstantMs)", "PresentationEasingMath.SmoothAndSnap(before.FundamentalRms, next.FundamentalRms, elapsedMilliseconds, PresentationTimeConstantMs, 1e-8, 8e-4)"),
    ("PresentationEasingMath.Smooth(before.ThdPercent, next.ThdPercent, elapsedMilliseconds, PresentationTimeConstantMs)", "PresentationEasingMath.SmoothAndSnap(before.ThdPercent, next.ThdPercent, elapsedMilliseconds, PresentationTimeConstantMs, 1e-5, 8e-4)"),
    ("PresentationEasingMath.Smooth(before.DominantRms, next.DominantRms, elapsedMilliseconds, PresentationTimeConstantMs)", "PresentationEasingMath.SmoothAndSnap(before.DominantRms, next.DominantRms, elapsedMilliseconds, PresentationTimeConstantMs, 1e-8, 8e-4)"),
    ("PresentationEasingMath.Smooth(before.DominantPercent, next.DominantPercent, elapsedMilliseconds, PresentationTimeConstantMs)", "PresentationEasingMath.SmoothAndSnap(before.DominantPercent, next.DominantPercent, elapsedMilliseconds, PresentationTimeConstantMs, 1e-5, 8e-4)"),
]:
    replace_once("Controls/ComtradeHarmonicsWorkstationView.cs", old, new)

# LOCUS workspace: static trajectories stay cached; native cursor results become latest graphical targets.
replace_once("Controls/ComtradeLocusView.cs", "using System.Globalization;\n", "using System.Diagnostics;\nusing System.Globalization;\n")
replace_once(
    "Controls/ComtradeLocusView.cs",
    "    private IReadOnlyList<ComtradeDistancePoint> _cursor1 = Array.Empty<ComtradeDistancePoint>();\n    private IReadOnlyList<ComtradeDistancePoint> _cursor2 = Array.Empty<ComtradeDistancePoint>();\n",
    "    private IReadOnlyList<ComtradeDistancePoint> _cursor1 = Array.Empty<ComtradeDistancePoint>();\n    private IReadOnlyList<ComtradeDistancePoint> _cursor2 = Array.Empty<ComtradeDistancePoint>();\n    private IReadOnlyList<ComtradeDistancePoint> _targetCursor1 = Array.Empty<ComtradeDistancePoint>();\n    private IReadOnlyList<ComtradeDistancePoint> _targetCursor2 = Array.Empty<ComtradeDistancePoint>();\n    private bool _cursorPresentationRenderingHooked;\n    private long _cursorPresentationTimestamp;\n    private const double CursorPresentationTimeConstantMs = 46.0;\n")
replace_once(
    "Controls/ComtradeLocusView.cs",
    "        MouseWheel += OnMouseWheel;\n",
    "        MouseWheel += OnMouseWheel;\n        Unloaded += (_, _) => StopCursorPresentationPump();\n")
replace_once(
    "Controls/ComtradeLocusView.cs",
    "        _cursor1 = Array.Empty<ComtradeDistancePoint>();\n        _cursor2 = Array.Empty<ComtradeDistancePoint>();\n        InvalidateStatic();\n",
    "        _cursor1 = Array.Empty<ComtradeDistancePoint>();\n        _cursor2 = Array.Empty<ComtradeDistancePoint>();\n        _targetCursor1 = Array.Empty<ComtradeDistancePoint>();\n        _targetCursor2 = Array.Empty<ComtradeDistancePoint>();\n        StopCursorPresentationPump();\n        InvalidateStatic();\n")
replace_between(
    "Controls/ComtradeLocusView.cs",
    "    internal void SetCursorPoints(\n",
    "    protected override void OnRender(DrawingContext dc)\n",
    '''    internal void SetCursorPoints(\n        IReadOnlyList<ComtradeDistancePoint>? cursor1,\n        IReadOnlyList<ComtradeDistancePoint>? cursor2)\n    {\n        _targetCursor1 = cursor1?.ToArray() ?? Array.Empty<ComtradeDistancePoint>();\n        _targetCursor2 = cursor2?.ToArray() ?? Array.Empty<ComtradeDistancePoint>();\n        if (!CursorTopologyMatches(_cursor1, _targetCursor1)) _cursor1 = _targetCursor1.ToArray();\n        if (!CursorTopologyMatches(_cursor2, _targetCursor2)) _cursor2 = _targetCursor2.ToArray();\n        if (CursorSetsSettled(_cursor1, _targetCursor1) && CursorSetsSettled(_cursor2, _targetCursor2))\n            StopCursorPresentationPump();\n        else\n            EnsureCursorPresentationPump();\n        InvalidateVisual();\n    }\n\n    private void EnsureCursorPresentationPump()\n    {\n        if (_cursorPresentationRenderingHooked) return;\n        _cursorPresentationTimestamp = Stopwatch.GetTimestamp();\n        CompositionTarget.Rendering += CursorPresentationFrame;\n        _cursorPresentationRenderingHooked = true;\n    }\n\n    private void StopCursorPresentationPump()\n    {\n        if (_cursorPresentationRenderingHooked)\n            CompositionTarget.Rendering -= CursorPresentationFrame;\n        _cursorPresentationRenderingHooked = false;\n        _cursorPresentationTimestamp = 0;\n    }\n\n    private void CursorPresentationFrame(object? sender, EventArgs e)\n    {\n        var now = Stopwatch.GetTimestamp();\n        var elapsedMilliseconds = _cursorPresentationTimestamp == 0\n            ? 16.67\n            : Math.Clamp(Stopwatch.GetElapsedTime(_cursorPresentationTimestamp, now).TotalMilliseconds, 1.0, 50.0);\n        _cursorPresentationTimestamp = now;\n        _cursor1 = SmoothCursorSet(_cursor1, _targetCursor1, elapsedMilliseconds);\n        _cursor2 = SmoothCursorSet(_cursor2, _targetCursor2, elapsedMilliseconds);\n        var settled = CursorSetsSettled(_cursor1, _targetCursor1) && CursorSetsSettled(_cursor2, _targetCursor2);\n        if (settled)\n        {\n            _cursor1 = _targetCursor1.ToArray();\n            _cursor2 = _targetCursor2.ToArray();\n        }\n        InvalidateVisual();\n        if (settled) StopCursorPresentationPump();\n    }\n\n    private static ComtradeDistancePoint[] SmoothCursorSet(\n        IReadOnlyList<ComtradeDistancePoint> current,\n        IReadOnlyList<ComtradeDistancePoint> target,\n        double elapsedMilliseconds)\n    {\n        if (!CursorTopologyMatches(current, target)) return target.ToArray();\n        var result = new ComtradeDistancePoint[target.Count];\n        for (var index = 0; index < target.Count; index++)\n        {\n            var before = current[index];\n            var next = target[index];\n            if (!before.Valid || !next.Valid)\n            {\n                result[index] = next;\n                continue;\n            }\n            result[index] = next with\n            {\n                R = PresentationEasingMath.SmoothAndSnap(before.R, next.R, elapsedMilliseconds, CursorPresentationTimeConstantMs, 1e-7, 6e-4),\n                X = PresentationEasingMath.SmoothAndSnap(before.X, next.X, elapsedMilliseconds, CursorPresentationTimeConstantMs, 1e-7, 6e-4),\n                Magnitude = PresentationEasingMath.SmoothAndSnap(before.Magnitude, next.Magnitude, elapsedMilliseconds, CursorPresentationTimeConstantMs, 1e-7, 6e-4),\n                AngleDegrees = PresentationEasingMath.SmoothAngleAndSnapDegrees(before.AngleDegrees, next.AngleDegrees, elapsedMilliseconds, CursorPresentationTimeConstantMs),\n                MeasuringCurrent = PresentationEasingMath.SmoothAndSnap(before.MeasuringCurrent, next.MeasuringCurrent, elapsedMilliseconds, CursorPresentationTimeConstantMs, 1e-7, 6e-4)\n            };\n        }\n        return result;\n    }\n\n    private static bool CursorTopologyMatches(IReadOnlyList<ComtradeDistancePoint> left, IReadOnlyList<ComtradeDistancePoint> right)\n    {\n        if (left.Count != right.Count || left.Count == 0) return left.Count == 0 && right.Count == 0;\n        for (var index = 0; index < left.Count; index++)\n            if (left[index].Loop != right[index].Loop) return false;\n        return true;\n    }\n\n    private static bool CursorSetsSettled(IReadOnlyList<ComtradeDistancePoint> current, IReadOnlyList<ComtradeDistancePoint> target)\n    {\n        if (!CursorTopologyMatches(current, target)) return false;\n        for (var index = 0; index < target.Count; index++)\n        {\n            if (current[index].Valid != target[index].Valid) return false;\n            if (!target[index].Valid) continue;\n            if (!PresentationEasingMath.IsSettled(current[index].R, target[index].R, 1e-7, 6e-4) ||\n                !PresentationEasingMath.IsSettled(current[index].X, target[index].X, 1e-7, 6e-4)) return false;\n        }\n        return true;\n    }\n\n''')

# Unit tests for deterministic settle/snap behavior; existing integration tests cover native COMTRADE authority.
replace_once(
    "tests/ARSAS.Tests/PresentationEasingMathTests.cs",
    "    [Fact]\n    public void InfiniteElapsedTime_SnapsToTarget()\n    {\n        Assert.Equal(1.0, PresentationEasingMath.ExponentialAlpha(double.PositiveInfinity, 80.0));\n        Assert.Equal(42.0, PresentationEasingMath.Smooth(10.0, 42.0, double.PositiveInfinity, 80.0), 10);\n    }\n",
    "    [Fact]\n    public void InfiniteElapsedTime_SnapsToTarget()\n    {\n        Assert.Equal(1.0, PresentationEasingMath.ExponentialAlpha(double.PositiveInfinity, 80.0));\n        Assert.Equal(42.0, PresentationEasingMath.Smooth(10.0, 42.0, double.PositiveInfinity, 80.0), 10);\n    }\n\n    [Fact]\n    public void SmoothAndSnap_ConvergesExactlyInsideTolerance()\n    {\n        var value = PresentationEasingMath.SmoothAndSnap(9.9999, 10.0, 16.67, 60.0, 0.001);\n        Assert.Equal(10.0, value, 12);\n        Assert.True(PresentationEasingMath.IsSettled(value, 10.0, 0.001));\n    }\n\n    [Fact]\n    public void AngleSmoothAndSnap_UsesShortestPathAndSettlesExactly()\n    {\n        var value = PresentationEasingMath.SmoothAngleAndSnapDegrees(179.99, -179.99, 16.67, 60.0, 0.05);\n        Assert.Equal(-179.99, value, 8);\n    }\n")

print("COMTRADE smoothing patch applied successfully")
