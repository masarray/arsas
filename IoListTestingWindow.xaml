<Window x:Class="ArIED61850Tester.IoListTestingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ARSAS - IO List Testing"
        Height="900" Width="1500" MinHeight="760" MinWidth="1180"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        Background="{StaticResource AppBackgroundGradient}"
        FontFamily="Aptos, Segoe UI Variable Text, Segoe UI, Calibri"
        Icon="Assets/app-icon.ico">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="IO LIST TESTING" Style="{StaticResource MicroLabel}" Foreground="{StaticResource Accent}"/>
                    <TextBlock Text="{Binding Project.ProjectName}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                    <TextBlock Text="Dedicated FAT workspace · imported IEDs and IO-list signals only"
                               Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource SoftBlue}" CornerRadius="14" Padding="12,7" Margin="0,0,8,0">
                        <TextBlock Text="{Binding ProjectSummary}" FontSize="12" Foreground="{StaticResource Ink}"/>
                    </Border>
                    <Button Content="Return to Engineering" Style="{StaticResource SoftButton}" Click="ReturnToEngineering_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="286"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="10">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="4,2,4,10">
                        <TextBlock Text="IED TEST PLAN" Style="{StaticResource MicroLabel}"/>
                        <TextBlock Text="Select one imported IED" Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Project.Ieds}"
                             SelectedItem="{Binding SelectedIed, Mode=TwoWay}"
                             BorderThickness="0" Background="Transparent"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,7"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="IedCard" Background="{StaticResource IedCardSurface}"
                                                    BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1"
                                                    CornerRadius="14" Padding="10">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="IedCard" Property="Background" Value="{StaticResource IedCardHover}"/>
                                                    <Setter TargetName="IedCard" Property="BorderBrush" Value="#8EAFE3"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="IedCard" Property="Background" Value="{StaticResource IedCardSelected}"/>
                                                    <Setter TargetName="IedCard" Property="BorderBrush" Value="#4F86E5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding IedName}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                    <TextBlock Grid.Row="1" Text="{Binding IpAddress}" Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                                    <TextBlock Grid.Row="2" Margin="0,6,0,0" FontSize="11.3" Foreground="{StaticResource Muted}">
                                        <Run Text="{Binding PassedCount}"/><Run Text=" passed · "/>
                                        <Run Text="{Binding ReviewCount}"/><Run Text=" review · "/>
                                        <Run Text="{Binding PendingCount}"/><Run Text=" pending"/>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedIed.IedName, TargetNullValue=Select an IED}" Style="{StaticResource SectionTitle}"/>
                            <TextBlock Text="{Binding SelectedIed.IedRole}" Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="#F7FAFF" BorderBrush="{StaticResource BorderSubtle}"
                                BorderThickness="1" CornerRadius="13" Padding="11,6">
                            <TextBlock Text="Only imported SDI signals are shown" Style="{StaticResource Caption}"/>
                        </Border>
                    </Grid>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding SelectedIed.TestPoints}"
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                              IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              BorderThickness="0" Background="Transparent" RowBackground="White"
                              AlternatingRowBackground="#F8FAFD" HorizontalGridLinesBrush="{StaticResource BorderSubtle}"
                              SelectionMode="Single" SelectionUnit="FullRow">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="Test" Binding="{Binding TestEnabled}" Width="54"/>
                            <DataGridTextColumn Header="Signal name" Binding="{Binding SignalName}" Width="2.2*"/>
                            <DataGridTextColumn Header="IEC reference" Binding="{Binding ObjectReference}" Width="2.5*"/>
                            <DataGridTextColumn Header="FC" Binding="{Binding FunctionalConstraint}" Width="58"/>
                            <DataGridTextColumn Header="ON evidence" Binding="{Binding Runtime.OnEvidence.CapturedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss.fff}}" Width="180"/>
                            <DataGridTextColumn Header="OFF evidence" Binding="{Binding Runtime.OffEvidence.CapturedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss.fff}}" Width="180"/>
                            <DataGridTextColumn Header="Result" Binding="{Binding Runtime.State}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Runtime.StatusReason}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="4" Background="#F7FAFF" BorderBrush="{StaticResource BorderSubtle}"
                BorderThickness="1" CornerRadius="14" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="The runtime, Excel importer, and evidence journal are connected in the next implementation tranche."
                           Style="{StaticResource Caption}"/>
                <TextBlock Grid.Column="1" Text="Read-only FAT foundation" Style="{StaticResource MicroLabel}" Foreground="{StaticResource Success}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
