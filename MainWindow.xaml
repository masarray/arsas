<Window x:Class="ArIED61850Tester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ArIED 61850 — Smart IED Explorer &amp; Monitor"
        Height="900" Width="1480" MinHeight="760" MinWidth="1180"
        WindowStartupLocation="CenterScreen" Background="{StaticResource AppBackgroundGradient}"
        FontFamily="Aptos, Segoe UI Variable Text, Segoe UI, Calibri"
        Icon="Assets/app-icon.ico" Closing="Window_Closing">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Compact navigation row: the large top application bar is removed to free vertical space. -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="WorkflowNavShell" Width="760" Height="56" HorizontalAlignment="Left"
                    Background="#E7ECF5" CornerRadius="20" Padding="5" Effect="{StaticResource SoftShadow}"
                    ClipToBounds="False">
                <Grid x:Name="WorkflowNavGrid" ClipToBounds="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="WorkflowPill" Grid.ColumnSpan="5" Width="148" Height="34"
                            HorizontalAlignment="Left" VerticalAlignment="Center" CornerRadius="17"
                            Background="{StaticResource Accent}" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="WorkflowPillScale" ScaleX="1" ScaleY="1"/>
                                <TranslateTransform x:Name="WorkflowPillTranslate" X="0"/>
                            </TransformGroup>
                        </Border.RenderTransform>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.18" Color="#2563EB"/>
                        </Border.Effect>
                    </Border>
                    <Button x:Name="NavExplorerButton" Grid.Column="0" Content="IEC 61850 Explorer" Tag="0" Click="NavButton_Click" Style="{StaticResource SegmentedNavButton}"/>
                    <Button x:Name="NavLiveButton" Grid.Column="1" Content="Live Monitor" Tag="1" Click="NavButton_Click" Style="{StaticResource SegmentedNavButton}"/>
                    <Button x:Name="NavEventsButton" Grid.Column="2" Content="Event Log" Tag="2" Click="NavButton_Click" Style="{StaticResource SegmentedNavButton}"/>
                    <Button x:Name="NavGooseButton" Grid.Column="3" Content="GOOSE Subscriber" Tag="3" Click="NavButton_Click" Style="{StaticResource SegmentedNavButton}"/>
                    <Button x:Name="NavDiagnosticsButton" Grid.Column="4" Tag="4" Click="NavButton_Click" Style="{StaticResource SegmentedNavButton}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Diagnostics" VerticalAlignment="Center"/>
                            <Border Visibility="{Binding DiagnosticsAlertVisibility}"
                                    Width="17" Height="17" CornerRadius="8.5" Margin="7,0,0,0"
                                    Background="#DC2626" BorderBrush="White" BorderThickness="1.5">
                                <TextBlock Text="!" Foreground="White" FontSize="10.5" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                            </Border>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <WrapPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Background="{StaticResource PremiumSurface}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="15" Padding="10,6" Margin="0,0,8,0">
                    <TextBlock Text="{Binding ConnectionInsightText}" FontSize="12.5" Foreground="{StaticResource Ink}"/>
                </Border>
                <Border Background="{StaticResource PremiumSurface}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="15" Padding="10,6" Margin="0,0,8,0">
                    <TextBlock Text="{Binding MonitoringInsightText}" FontSize="12.5" Foreground="{StaticResource Ink}"/>
                </Border>
                <Border Background="{StaticResource PremiumSurface}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="15" Padding="10,6">
                    <TextBlock Text="{Binding EventInsightText}" FontSize="12.5" Foreground="{StaticResource Ink}"/>
                </Border>
            </WrapPanel>
        </Grid>

        <TabControl x:Name="MainTabs" Grid.Row="1" SelectedIndex="0"
                    Style="{StaticResource HiddenTabControl}"
                    SelectionChanged="MainTabs_SelectionChanged">
            <!-- IEC 61850 EXPLORER -->
            <TabItem Header="IEC 61850 Explorer">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="258"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource Card}" Padding="10">
                        <DockPanel>
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource Accent}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="IED Explorer" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Style="{StaticResource IedIconButton}" Click="OpenProject_Click" ToolTip="Open ArIED project" Margin="0,0,6,0">
                                        <Viewbox Width="16" Height="16">
                                            <Path Data="{StaticResource LucideFolderOpen}" Style="{StaticResource LucideIcon}"/>
                                        </Viewbox>
                                    </Button>
                                    <Button Style="{StaticResource IedIconButton}" Click="SaveProject_Click" ToolTip="Save ArIED project">
                                        <Viewbox Width="16" Height="16">
                                            <Path Data="{StaticResource LucideSave}" Style="{StaticResource LucideIcon}"/>
                                        </Viewbox>
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <!-- Always-visible fast workflow actions. They remain anchored below the
                                 independently scrolling IED list and never move with its content. -->
                            <Border DockPanel.Dock="Bottom" Margin="0,8,0,0" Padding="5"
                                    Background="#F7FAFF" BorderBrush="#DCE6F5" BorderThickness="1"
                                    CornerRadius="15" Effect="{StaticResource SoftShadow}" Panel.ZIndex="30">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="6"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="6"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Row="0" Grid.ColumnSpan="3" Click="ConnectAllIeds_Click"
                                            Style="{StaticResource SoftButton}" Padding="8,7" FontSize="11.5"
                                            ToolTip="Fast-connect every saved or SCL-imported IED and start live monitoring for saved selections">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Viewbox Width="13" Height="13" Margin="0,0,5,0">
                                                <Path Data="{StaticResource LucidePlay}" Style="{StaticResource LucideIcon}" Stroke="#16A34A"/>
                                            </Viewbox>
                                            <TextBlock Text="Connect All" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Grid.Row="2" Grid.Column="0" Click="OpenScl_Click"
                                            Style="{StaticResource SoftButton}" Padding="7,7" FontSize="11.2"
                                            ToolTip="Import one or more IED endpoints from SCD, CID, ICD, IID, SSD, or XML SCL files">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Viewbox Width="13" Height="13" Margin="0,0,5,0">
                                                <Path Data="{StaticResource LucideFileInput}" Style="{StaticResource LucideIcon}"/>
                                            </Viewbox>
                                            <TextBlock Text="Open SCL" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Grid.Row="2" Grid.Column="2" Click="AddRelay_Click"
                                            Style="{StaticResource PrimaryButton}" Padding="7,7" FontSize="11.2"
                                            ToolTip="Add an IEC 61850 IED by IP address">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Viewbox Width="13" Height="13" Margin="0,0,5,0">
                                                <Path Data="{StaticResource LucidePlus}" Style="{StaticResource LucideIcon}" Stroke="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Add IED" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>

                            <ListBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
                                     BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0,0,0,8"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="Card" Background="{StaticResource IedCardSurface}"
                                                            BorderBrush="#CDD9EA" BorderThickness="1" CornerRadius="16" Padding="9">
                                                        <Border.Effect><DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.08" Color="#1E3355"/></Border.Effect>
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding IsMonitoring}" Value="True">
                                                            <Setter TargetName="Card" Property="Background" Value="{StaticResource IedCardConnected}"/>
                                                            <Setter TargetName="Card" Property="BorderBrush" Value="#83C7A1"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Card" Property="Background" Value="{StaticResource IedCardSelected}"/>
                                                            <Setter TargetName="Card" Property="BorderBrush" Value="#6F9FF1"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Card" Property="Background" Value="{StaticResource IedCardHover}"/>
                                                            <Setter TargetName="Card" Property="BorderBrush" Value="#8EAFE3"/>
                                                        </Trigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding IsMonitoring}" Value="True"/>
                                                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}" Value="True"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter TargetName="Card" Property="Background" Value="{StaticResource IedCardConnectedSelected}"/>
                                                            <Setter TargetName="Card" Property="BorderBrush" Value="#55A77E"/>
                                                        </MultiDataTrigger>
                                                        <MultiTrigger>
                                                            <MultiTrigger.Conditions>
                                                                <Condition Property="IsSelected" Value="True"/>
                                                                <Condition Property="IsMouseOver" Value="True"/>
                                                            </MultiTrigger.Conditions>
                                                            <Setter TargetName="Card" Property="Background" Value="{StaticResource IedCardSelected}"/>
                                                            <Setter TargetName="Card" Property="BorderBrush" Value="#4F86E5"/>
                                                        </MultiTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinHeight="70">
                                            <Grid x:Name="CardContent">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="56"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- Compact relay / BCU icon; the full SVG and glow stay inside the card. -->
                                      <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Width="52" Height="52" Margin="1,0,3,0"
                                            VerticalAlignment="Center" ClipToBounds="False" Panel.ZIndex="2">
                                          <Viewbox Width="44" Height="44" Stretch="Uniform"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                              <Path x:Name="RelayDeviceIcon" Data="M 2 0 L 2 20 L 18 20 L 18 0 L 2 0 z M 3 1 L 17 1 L 17 19 L 3 19 L 3 1 z M 5 3 L 5 7 L 15 7 L 15 3 L 5 3 z M 6 4 L 14 4 L 14 6 L 6 6 L 6 4 z M 5 10 L 5 11 L 7 11 L 7 10 L 5 10 z M 9 10 L 9 11 L 11 11 L 11 10 L 9 10 z M 13 10 L 13 11 L 15 11 L 15 10 L 13 10 z M 5 13 L 5 14 L 7 14 L 7 13 L 5 13 z M 9 13 L 9 14 L 11 14 L 11 13 L 9 13 z M 13 13 L 13 14 L 15 14 L 15 13 L 13 13 z M 5 16 L 5 17 L 7 17 L 7 16 L 5 16 z M 9 16 L 9 17 L 11 17 L 11 16 L 9 16 z M 13 16 L 13 17 L 15 17 L 15 16 L 13 16 z" Fill="#EF4444" Stretch="Uniform">
                                                  <Path.Effect><DropShadowEffect BlurRadius="7" ShadowDepth="0" Opacity="0.38" Color="#EF4444"/></Path.Effect>
                                              </Path>
                                          </Viewbox>
                                      </Grid>

                                      <Border x:Name="MonitorStateBadge" Grid.Row="0" Grid.Column="0"
                                              HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-7,4,0"
                                              Background="#FFF0F1" BorderBrush="#F3A3A8" BorderThickness="1"
                                              CornerRadius="9" Padding="6,2" Panel.ZIndex="5">
                                          <StackPanel Orientation="Horizontal">
                                              <Ellipse x:Name="MonitorStateDot" Width="5" Height="5" Fill="#E5484D" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                              <TextBlock x:Name="MonitorStateText" Text="STOP" FontSize="8.4" FontWeight="Bold" Foreground="#C92A32"/>
                                          </StackPanel>
                                      </Border>

<StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,0,12,0">
                                                    <TextBlock Text="{Binding Name}" FontSize="12.8" FontWeight="SemiBold"
                                                               Foreground="{StaticResource Ink}" TextOptions.TextFormattingMode="Display" TextTrimming="CharacterEllipsis"
                                                               TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                                    <TextBlock Text="{Binding EndpointText}" FontSize="11.1"
                                                               Foreground="{StaticResource Muted}" TextOptions.TextFormattingMode="Display" Margin="0,2,0,0"
                                                               TextTrimming="CharacterEllipsis" TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                                </StackPanel>

                                                <!-- Direct, unobstructed hit targets below the IP address. -->
                                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal"
                                                            HorizontalAlignment="Right" Margin="0,4,7,0" Panel.ZIndex="10">
                                                    <Button Style="{StaticResource IedIconButton}" Tag="{Binding}" Click="IedPlayAction_Click"
                                                            IsEnabled="{Binding CanPlayAction}" ToolTip="{Binding PlayToolTip}">
                                                        <Viewbox Width="16" Height="16"><Path Data="{StaticResource LucidePlay}" Style="{StaticResource LucideIcon}" Stroke="#16A34A"/></Viewbox>
                                                    </Button>
                                                    <Button Style="{StaticResource IedIconButton}" Tag="{Binding}" Click="IedStopAction_Click"
                                                            IsEnabled="{Binding CanStopAction}" ToolTip="{Binding StopToolTip}">
                                                        <Viewbox Width="16" Height="16"><Path Data="{StaticResource LucideSquare}" Style="{StaticResource LucideIcon}" Stroke="{StaticResource Warning}"/></Viewbox>
                                                    </Button>
                                                    <Button Style="{StaticResource IedIconButton}" Tag="{Binding}" Click="IedConfigureSignals_Click"
                                                            IsEnabled="{Binding CanEditSignals}" ToolTip="{Binding ConfigureToolTip}">
                                                        <Viewbox Width="16" Height="16"><Path Data="{StaticResource LucidePencilLine}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                                    </Button>
                                                    <Button Style="{StaticResource IedIconButton}" Tag="{Binding}" Click="IedSaveScl_Click"
                                                            IsEnabled="{Binding CanSaveScl}" ToolTip="{Binding SaveSclToolTip}">
                                                        <Viewbox Width="16" Height="16"><Path Data="{StaticResource LucideSave}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>

                                            <Button Style="{StaticResource IedIconButton}" Tag="{Binding}" Click="IedRemove_Click"
                                                    IsEnabled="{Binding IsActionEnabled}" ToolTip="{Binding RemoveToolTip}"
                                                    Width="25" Height="25" Margin="0,-4,-4,0"
                                                    HorizontalAlignment="Right" VerticalAlignment="Top" Panel.ZIndex="12">
                                                <Viewbox Width="14" Height="14"><Path Data="{StaticResource LucideX}" Style="{StaticResource LucideIcon}" Stroke="{StaticResource Danger}"/></Viewbox>
                                            </Button>

                                            <Border x:Name="BusyOverlay" Visibility="Collapsed" Background="#F7FAFEF7" CornerRadius="12"
                                                    BorderBrush="#B8CEF3" BorderThickness="1" Padding="8,6" Panel.ZIndex="20">
                                                <Grid Height="56">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="32"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                                        <Grid.RenderTransform><RotateTransform x:Name="CardSpinnerRotate"/></Grid.RenderTransform>
                                                        <Canvas Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.88"><Path.RenderTransform><RotateTransform Angle="45" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.76"><Path.RenderTransform><RotateTransform Angle="90" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.64"><Path.RenderTransform><RotateTransform Angle="135" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.52"><Path.RenderTransform><RotateTransform Angle="180" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.40"><Path.RenderTransform><RotateTransform Angle="225" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.28"><Path.RenderTransform><RotateTransform Angle="270" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                            <Path Data="M12,1.5 L12,5" Stroke="{StaticResource Accent}" StrokeThickness="2.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.18"><Path.RenderTransform><RotateTransform Angle="315" CenterX="12" CenterY="12"/></Path.RenderTransform></Path>
                                                        </Canvas>
                                                        <Grid.Triggers>
                                                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                                                <BeginStoryboard>
                                                                    <Storyboard RepeatBehavior="Forever">
                                                                        <DoubleAnimation Storyboard.TargetName="CardSpinnerRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.9"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </EventTrigger>
                                                        </Grid.Triggers>
                                                    </Grid>
                                                    <Grid Grid.Column="2" VerticalAlignment="Center">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Text="{Binding BusyTitle}" FontSize="11.8" FontWeight="SemiBold" Foreground="{StaticResource Ink}"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Grid.Row="1" Text="{Binding BusyStage}" FontSize="10.2" Foreground="{StaticResource Muted}"
                                                                   TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                                        <Grid Grid.Row="2" Margin="0,5,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <ProgressBar Value="{Binding DiscoveryProgressPercent, Mode=OneWay}" Minimum="0" Maximum="100"
                                                                         Style="{StaticResource DiscoveryProgressBar}" Height="5" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding DiscoveryProgressPercentText}" FontSize="9.6" FontWeight="SemiBold"
                                                                       Foreground="{StaticResource Accent}" Margin="7,0,0,0" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsMonitoring}" Value="True">
                                                <Setter TargetName="RelayDeviceIcon" Property="Fill" Value="#16A34A"/>
                                                <Setter TargetName="RelayDeviceIcon" Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.40" Color="#16A34A"/></Setter.Value></Setter>
                                                <Setter TargetName="MonitorStateBadge" Property="Background" Value="#E5F7ED"/>
                                                <Setter TargetName="MonitorStateBadge" Property="BorderBrush" Value="#8CCDA8"/>
                                                <Setter TargetName="MonitorStateDot" Property="Fill" Value="{StaticResource Success}"/>
                                                <Setter TargetName="MonitorStateText" Property="Text" Value="LIVE"/>
                                                <Setter TargetName="MonitorStateText" Property="Foreground" Value="{StaticResource Success}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                <Setter TargetName="CardContent" Property="Opacity" Value="0.10"/>
                                                <Setter TargetName="CardContent" Property="IsHitTestVisible" Value="False"/>
                                                <Setter TargetName="BusyOverlay" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Border>

                    <Grid Grid.Column="2">
                        <!-- Original premium first-run empty state restored. -->
                        <Border Style="{StaticResource Card}" Padding="0" Visibility="{Binding EmptyExplorerVisibility}" ClipToBounds="True">
                            <Grid>
                                <Image Source="Assets/gateway-hero.png" Stretch="UniformToFill" Opacity="0.88"/>
                                <Rectangle Fill="#F8FAFC" Opacity="0.18"/>
                                <Border Background="#F8FAFC" Opacity="0.91" CornerRadius="22" Padding="22"
                                        HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="34" MaxWidth="535">
                                    <StackPanel>
                                        <TextBlock Text="Start by adding your first IED" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                        <TextBlock Text="Connect a relay by IP, let ArIED discover the live IEC 61850 MMS model, choose only the signals you need, then monitor every IED independently."
                                                   TextWrapping="Wrap" FontSize="13.4" Foreground="{StaticResource Muted}" Margin="0,10,0,18"/>
                                        <WrapPanel>
                                            <Button Style="{StaticResource SoftButton}" Click="OpenProject_Click" Margin="0,0,10,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <Viewbox Width="15" Height="15" Margin="0,0,7,0"><Path Data="{StaticResource LucideFolderOpen}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                                    <TextBlock Text="Open Project"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBlock Text="Use the always-visible Add IED action below the Explorer list."
                                                       Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <Grid Visibility="{Binding SelectedExplorerVisibility}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Style="{StaticResource Card}" Padding="10">
                                <Grid>
                                    <DataGrid ItemsSource="{Binding SelectedDevice.Points}" IsReadOnly="True"
                                              Style="{StaticResource ModernDataGrid}" FrozenColumnCount="2"
                                              EnableRowVirtualization="True" EnableColumnVirtualization="True"
                                              VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                              ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                        <DataGrid.RowStyle>
                                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRecentlyChanged}" Value="True">
                                                        <Setter Property="Background" Value="#FFF0A8"/>
                                                        <Setter Property="BorderBrush" Value="#F59E0B"/>
                                                        <Setter Property="BorderThickness" Value="4,1,1,1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.RowStyle>
                                        <DataGrid.CellStyle>
                                            <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRecentlyChanged}" Value="True">
                                                        <Setter Property="Background" Value="#FFF5C2"/>
                                                        <Setter Property="Foreground" Value="#5C3B00"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.CellStyle>
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Signal" Binding="{Binding SignalName}" Width="1.05*" MinWidth="145"/>
                                            <DataGridTextColumn Header="IEC Telegram" Binding="{Binding IecTelegram}" Width="1.55*" MinWidth="205"/>
                                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="0.8*" MinWidth="125"/>
                                            <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="0.72*" MinWidth="105"/>
                                            <DataGridTextColumn Header="IED Timestamp" Binding="{Binding DeviceTimestamp}" Width="1.05*" MinWidth="155"/>
                                            <DataGridTextColumn Header="Acquisition" Binding="{Binding SourceMode}" Width="1.05*" MinWidth="150"/>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <Border Visibility="{Binding SelectedDeviceNoLivePointsVisibility}" Background="#FAFCFF" CornerRadius="16"
                                            BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="24"
                                            HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
                                        <StackPanel>
                                            <TextBlock Text="{Binding SelectedDevice.ReadyWorkspaceTitle}" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource Ink}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding SelectedDevice.ReadyWorkspaceMessage}"
                                                       TextWrapping="Wrap" TextAlignment="Center" FontSize="13" Foreground="{StaticResource Muted}" Margin="0,8,0,16"/>
                                            <WrapPanel HorizontalAlignment="Center">
                                                <Button Content="Edit Signals" Tag="{Binding SelectedDevice}" Click="IedConfigureSignals_Click"
                                                        Style="{StaticResource SoftButton}" Margin="0,0,8,0" IsEnabled="{Binding SelectedDevice.CanEditSignals}"/>
                                                <Button Content="{Binding SelectedDevice.MonitorActionLabel}" Tag="{Binding SelectedDevice}" Click="IedMonitorAction_Click"
                                                        Style="{StaticResource PrimaryButton}" IsEnabled="{Binding SelectedDevice.CanStartOrStopMonitor}"/>
                                            </WrapPanel>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <Expander x:Name="CommandPanelExpander" Grid.Row="1" Margin="0,10,0,0" IsExpanded="False">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="28" Height="28" CornerRadius="9" Background="#E7F0FF" BorderBrush="#C9DBF7" BorderThickness="1" Margin="0,0,9,0">
                                            <Viewbox Width="15" Height="15" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Path Data="{StaticResource LucideSlidersHorizontal}" Style="{StaticResource LucideIcon}"/>
                                            </Viewbox>
                                        </Border>
                                        <TextBlock Text="IED Command Panel" FontSize="13.6" FontWeight="SemiBold" Foreground="{StaticResource Ink}" VerticalAlignment="Center"/>
                                        <Border Background="#EAF2FF" BorderBrush="#C7D8F2" BorderThickness="1" CornerRadius="10" Padding="7,2" Margin="9,0,0,0">
                                            <TextBlock Text="{Binding SelectedDevice.CommandSignals.Count, StringFormat={}{0} controls}" FontSize="10.8" FontWeight="SemiBold" Foreground="#45648E"/>
                                        </Border>
                                    </StackPanel>
                                </Expander.Header>
                                <Border Style="{StaticResource Card}" Background="{StaticResource CommandPanelSurface}" Padding="16" BorderBrush="#BFD0E5">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <Border Background="{StaticResource CommandHeaderSurface}" BorderBrush="#CDDCEE" BorderThickness="1"
                                                CornerRadius="13" Padding="13,10" Margin="0,0,0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="Fast IEC 61850 Control" FontSize="14.2" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                                    <TextBlock Text="Operate only the validated control objects selected for this IED."
                                                               FontSize="11.2" Foreground="{StaticResource Muted}" Margin="0,3,0,0"/>
                                                </StackPanel>
                                                <WrapPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                    <Button Click="RefreshCommandValues_Click" Style="{StaticResource SoftButton}" Padding="11,6" Margin="0,0,8,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Viewbox Width="14" Height="14" Margin="0,0,6,0"><Path Data="{StaticResource LucideRefreshCw}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                                            <TextBlock Text="Refresh values"/>
                                                        </StackPanel>
                                                    </Button>
                                                    <Button Tag="{Binding SelectedDevice}" Click="IedConfigureSignals_Click"
                                                            Style="{StaticResource SoftButton}" Padding="11,6" IsEnabled="{Binding SelectedDevice.CanEditSignals}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Viewbox Width="14" Height="14" Margin="0,0,6,0"><Path Data="{StaticResource LucidePencilLine}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                                            <TextBlock Text="Edit signals"/>
                                                        </StackPanel>
                                                    </Button>
                                                </WrapPanel>
                                            </Grid>
                                        </Border>

                                        <Grid Grid.Row="1">
                                            <DataGrid ItemsSource="{Binding SelectedDevice.CommandSignals}" IsReadOnly="False" CanUserAddRows="False" CanUserDeleteRows="False"
                                                      Style="{StaticResource CommandDataGrid}" RowStyle="{StaticResource CommandDataGridRow}"
                                                      CellStyle="{StaticResource CommandDataGridCell}" ColumnHeaderStyle="{StaticResource CommandDataGridHeader}"
                                                      AutoGenerateColumns="False" MaxHeight="320" MinHeight="56" RowHeight="48" MinRowHeight="46" FrozenColumnCount="1"
                                                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                                                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                                      ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                      CanUserResizeColumns="False" ClipToBounds="True">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="IEC Telegram" Binding="{Binding DisplayReference}" Width="1.9*" MinWidth="145" IsReadOnly="True"/>
                                                    <DataGridTemplateColumn Header="Value" Width="0.78*" MinWidth="88">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <Border Padding="9,5" CornerRadius="10" HorizontalAlignment="Left" MinWidth="76">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="Background" Value="#F2F4F7"/>
                                                                            <Setter Property="BorderBrush" Value="#D0D5DD"/>
                                                                            <Setter Property="BorderThickness" Value="1"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Energized">
                                                                                    <Setter Property="Background" Value="#FEF3F2"/>
                                                                                    <Setter Property="BorderBrush" Value="#FECDCA"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Deenergized">
                                                                                    <Setter Property="Background" Value="#ECFDF3"/>
                                                                                    <Setter Property="BorderBrush" Value="#ABEFC6"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Abnormal">
                                                                                    <Setter Property="Background" Value="#FFFAEB"/>
                                                                                    <Setter Property="BorderBrush" Value="#FEDF89"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <TextBlock Text="{Binding ControlCurrentValue}" FontWeight="SemiBold" HorizontalAlignment="Center">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Foreground" Value="#344054"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Energized"><Setter Property="Foreground" Value="#B42318"/></DataTrigger>
                                                                                    <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Deenergized"><Setter Property="Foreground" Value="#067647"/></DataTrigger>
                                                                                    <DataTrigger Binding="{Binding ControlCurrentTone}" Value="Abnormal"><Setter Property="Foreground" Value="#B54708"/></DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Border>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                    <DataGridTextColumn Header="CDC / Type" Binding="{Binding ControlCdc}" Width="0.7*" MinWidth="74" IsReadOnly="True"/>
                                                    <DataGridTextColumn Header="Control model" Binding="{Binding ControlModelText}" Width="1.15*" MinWidth="130" IsReadOnly="True"/>
                                                    <DataGridTemplateColumn Header="Checks" Width="1.22*" MinWidth="165">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2">
                                                                    <CheckBox Content="Interlock" FontSize="11.6"
                                                                              IsChecked="{Binding ControlInterlockCheck, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              Margin="0,0,10,0"
                                                                              ToolTip="Set the IEC 61850 interlock check flag for this signal only."/>
                                                                    <CheckBox Content="Sync" FontSize="11.6"
                                                                              IsChecked="{Binding ControlSynchroCheck, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              Margin="0,0,10,0"
                                                                              ToolTip="Set the IEC 61850 synchrocheck flag for this signal only."/>
                                                                    <CheckBox Content="Test" FontSize="11.6"
                                                                              IsChecked="{Binding ControlTestMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              ToolTip="Send this signal in IEC 61850 Test mode."/>
                                                                </WrapPanel>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                    <DataGridTemplateColumn Header="Control" Width="1.55*" MinWidth="190">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <StackPanel Margin="0,3">
                                                                    <Grid>
                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding IsPositionControl}" Value="True"/>
                                                                                                <Condition Binding="{Binding ControlConfirmationPending}" Value="False"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="Open" Tag="{Binding}" CommandParameter="Open [01]" Click="ControlStageAction_Click"
                                                                                    Style="{StaticResource CommandOpenButton}" Margin="0,0,7,0"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                            <Button Content="Close" Tag="{Binding}" CommandParameter="Closed [10]" Click="ControlStageAction_Click"
                                                                                    Style="{StaticResource CommandCloseButton}" Margin="0,0,7,0"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding IsPositionControl}" Value="True"/>
                                                                                                <Condition Binding="{Binding ControlConfirmationPending}" Value="True"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="{Binding ControlPendingConfirmationLabel}" Tag="{Binding}" Click="ControlConfirmAction_Click"
                                                                                    Style="{StaticResource CommandConfirmButton}"
                                                                                    IsEnabled="{Binding ControlCanConfirm}" Margin="0,0,7,0"/>
                                                                            <Button Content="Cancel" Tag="{Binding}" Click="ControlCancelAction_Click"
                                                                                    Style="{StaticResource SoftButton}" Padding="12,6"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsRaiseOnlyControl}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="Raise" Tag="{Binding}" CommandParameter="Raise" Click="ControlQuickAction_Click" Style="{StaticResource PrimaryButton}" Padding="14,6"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsLowerOnlyControl}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="Lower" Tag="{Binding}" CommandParameter="Lower" Click="ControlQuickAction_Click" Style="{StaticResource SoftButton}" Padding="14,6"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsRaiseLowerControl}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="Raise" Tag="{Binding}" CommandParameter="Raise" Click="ControlQuickAction_Click" Style="{StaticResource PrimaryButton}" Padding="14,6" Margin="0,0,7,0"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                            <Button Content="Lower" Tag="{Binding}" CommandParameter="Lower" Click="ControlQuickAction_Click" Style="{StaticResource SoftButton}" Padding="14,6"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsBooleanControl}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <Button Content="True" Tag="{Binding}" CommandParameter="True" Click="ControlQuickAction_Click" Style="{StaticResource CommandCloseButton}" Margin="0,0,7,0"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                            <Button Content="False" Tag="{Binding}" CommandParameter="False" Click="ControlQuickAction_Click" Style="{StaticResource CommandOpenButton}"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>

                                                                        <StackPanel Orientation="Horizontal">
                                                                            <StackPanel.Style>
                                                                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsSetPointControl}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                                                </Style>
                                                                            </StackPanel.Style>
                                                                            <TextBox Text="{Binding ControlSetPointText, UpdateSourceTrigger=PropertyChanged}" Width="105" Height="31" Padding="8,4" Margin="0,0,7,0" ToolTip="Target value"/>
                                                                            <Button Content="Set" Tag="{Binding}" Click="ControlQuickAction_Click" Style="{StaticResource PrimaryButton}" Padding="14,6"
                                                                                    IsEnabled="{Binding ControlSupportsOperate}"/>
                                                                        </StackPanel>
                                                                    </Grid>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                            </DataGrid>

                                            <Border Background="#F8FBFF" BorderBrush="#C9D8EB" BorderThickness="1" CornerRadius="13" Padding="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding SelectedDevice.CommandSignals.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="No operable commands" FontWeight="SemiBold"
                                                           Foreground="{StaticResource Muted}" HorizontalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Expander>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- GLOBAL MULTI-IED LIVE MONITOR -->
            <TabItem Header="Live Monitor">
                <Border Style="{StaticResource Card}" Padding="12">
                    <DockPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Global Multi-IED Live Monitor" FontSize="15.5" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                <TextBlock Text="Every IED keeps its own connection, report subscription, validation reads, and start/stop state."
                                           FontSize="12.2" Foreground="{StaticResource Muted}" Margin="0,3,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#F8FAFC" BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="15" Padding="10,6">
                                <TextBlock Text="{Binding MonitoringInsightText}" FontSize="12.4" Foreground="{StaticResource Ink}"/>
                            </Border>
                        </Grid>
                        <DataGrid ItemsSource="{Binding GlobalPoints}" IsReadOnly="True" Style="{StaticResource ModernDataGrid}"
                                  FrozenColumnCount="3" EnableRowVirtualization="True" EnableColumnVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecentlyChanged}" Value="True">
                                            <Setter Property="Background" Value="#FFF0A8"/>
                                            <Setter Property="BorderBrush" Value="#F59E0B"/>
                                            <Setter Property="BorderThickness" Value="4,1,1,1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecentlyChanged}" Value="True">
                                            <Setter Property="Background" Value="#FFF5C2"/>
                                            <Setter Property="Foreground" Value="#5C3B00"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="IED" Binding="{Binding DeviceName}" Width="120"/>
                                <DataGridTextColumn Header="Signal" Binding="{Binding SignalName}" Width="195"/>
                                <DataGridTextColumn Header="IEC Telegram" Binding="{Binding IecTelegram}" Width="320"/>
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="125"/>
                                <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="155"/>
                                <DataGridTextColumn Header="IED Timestamp" Binding="{Binding DeviceTimestamp}" Width="185"/>
                                <DataGridTextColumn Header="Acquisition" Binding="{Binding SourceMode}" Width="190"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </TabItem>

            <!-- EVENT LOG -->
            <TabItem Header="Event Log">
                <Border Style="{StaticResource Card}" Padding="12">
                    <DockPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="IEC 61850 Sequence of Events" FontSize="15.5" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                <TextBlock Text="SCADA/SAS SOE: each row shows the new process value directly. Closed/ON/true is red, Open/OFF/false is green, and intermediate/bad states are amber. Non-events stay out."
                                           FontSize="12.2" Foreground="{StaticResource Muted}" Margin="0,3,0,0"/>
                            </StackPanel>
                            <WrapPanel Grid.Column="1">
                                <Button Content="Export CSV" Style="{StaticResource SoftButton}" Click="ExportEvents_Click" Margin="0,0,8,0"/>
                                <Button Content="Clear" Style="{StaticResource SoftButton}" Click="ClearEvents_Click"/>
                            </WrapPanel>
                        </Grid>
                        <DataGrid ItemsSource="{Binding Events}" IsReadOnly="True" Style="{StaticResource ModernDataGrid}"
                                  FrozenColumnCount="4" EnableRowVirtualization="True" EnableColumnVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding Sequence}" Width="65"/>
                                <DataGridTextColumn Header="IED Timestamp" Binding="{Binding DeviceTimestamp}" Width="185"/>
                                <DataGridTextColumn Header="IED" Binding="{Binding DeviceName}" Width="120"/>
                                <DataGridTextColumn Header="Signal" Binding="{Binding SignalName}" Width="190"/>
                                <DataGridTextColumn Header="IEC Telegram" Binding="{Binding IecTelegram}" Width="300"/>
                                <DataGridTemplateColumn Header="Value" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border x:Name="ValueBadge" CornerRadius="9" Padding="10,4" HorizontalAlignment="Left"
                                                    Background="#F1F5F9" BorderBrush="#D8E0EA" BorderThickness="1">
                                                <TextBlock x:Name="ValueText" Text="{Binding EventValue}" FontWeight="SemiBold" Foreground="#344054"/>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding ValueTone}" Value="Energized">
                                                    <Setter TargetName="ValueBadge" Property="Background" Value="#FEE4E2"/>
                                                    <Setter TargetName="ValueBadge" Property="BorderBrush" Value="#FDA29B"/>
                                                    <Setter TargetName="ValueText" Property="Foreground" Value="#B42318"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ValueTone}" Value="Deenergized">
                                                    <Setter TargetName="ValueBadge" Property="Background" Value="#ECFDF3"/>
                                                    <Setter TargetName="ValueBadge" Property="BorderBrush" Value="#86EFAC"/>
                                                    <Setter TargetName="ValueText" Property="Foreground" Value="#067647"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ValueTone}" Value="Abnormal">
                                                    <Setter TargetName="ValueBadge" Property="Background" Value="#FFFAEB"/>
                                                    <Setter TargetName="ValueBadge" Property="BorderBrush" Value="#FEC84B"/>
                                                    <Setter TargetName="ValueText" Property="Foreground" Value="#B54708"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="150"/>
                                <DataGridTextColumn Header="Acquisition" Binding="{Binding SourceMode}" Width="190"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </TabItem>

            <!-- SCL / DISCOVERY-AWARE GOOSE SUBSCRIBER -->
            <TabItem Header="GOOSE Subscriber">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="0.9*" MinHeight="205"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="1.1*" MinHeight="230"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Padding="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="235"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="8" Height="8" Fill="#16A34A" Margin="0,0,8,0"/>
                                    <TextBlock Text="GOOSE Subscriber" FontSize="14.5" FontWeight="SemiBold" Foreground="{StaticResource Ink}" VerticalAlignment="Center"/>
                                    <Border Margin="8,0,0,0" Background="#EEF4FF" BorderBrush="#C9D9F1" BorderThickness="1" CornerRadius="8" Padding="6,2"
                                            ToolTip="Read-only IEC 61850-8-1 Ethernet capture">
                                        <TextBlock Text="READ ONLY" FontSize="8.7" FontWeight="Bold" Foreground="#45648E"/>
                                    </Border>
                                </StackPanel>

                                <ComboBox Grid.Column="2" ItemsSource="{Binding GooseAdapters}" SelectedItem="{Binding SelectedGooseAdapter, Mode=TwoWay}"
                                          DisplayMemberPath="DisplayText" Style="{StaticResource ModernComboBox}" MinHeight="34" Height="34" Padding="10,4"
                                          IsEnabled="{Binding CanRefreshGooseConfiguration}" ToolTip="{Binding SelectedGooseAdapterDetail}"/>
                                <Button Grid.Column="4" Style="{StaticResource IedIconButton}" Click="RefreshGooseAdapters_Click"
                                        IsEnabled="{Binding CanRefreshGooseConfiguration}" ToolTip="Refresh adapters">
                                    <Viewbox Width="15" Height="15"><Path Data="{StaticResource LucideRefreshCw}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                </Button>

                                <TextBox Grid.Column="6" Text="{Binding GooseCaptureFilter, UpdateSourceTrigger=PropertyChanged}"
                                         MinHeight="34" Height="34" Padding="10,4" FontFamily="Cascadia Mono, Consolas" FontSize="10.2"
                                         IsEnabled="{Binding CanRefreshGooseConfiguration}" ToolTip="GOOSE BPF capture filter"/>
                                <Button Grid.Column="8" Style="{StaticResource IedIconButton}" Click="RefreshGooseModels_Click"
                                        IsEnabled="{Binding CanRefreshGooseConfiguration}" ToolTip="{Binding GooseBindingText}">
                                    <Viewbox Width="15" Height="15"><Path Data="{StaticResource LucideSlidersHorizontal}" Style="{StaticResource LucideIcon}"/></Viewbox>
                                </Button>

                                <Button Grid.Column="10" Style="{StaticResource CommandOpenButton}" Click="StartGooseSubscriber_Click"
                                        IsEnabled="{Binding CanStartGooseSubscriber}" Padding="13,6" MinHeight="34">
                                    <StackPanel Orientation="Horizontal">
                                        <Viewbox Width="13" Height="13" Margin="0,0,6,0"><Path Data="{StaticResource LucidePlay}" Style="{StaticResource LucideIcon}" Stroke="White"/></Viewbox>
                                        <TextBlock Text="Start" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="12" Style="{StaticResource SoftButton}" Click="StopGooseSubscriber_Click"
                                        IsEnabled="{Binding CanStopGooseSubscriber}" Padding="11,6" MinHeight="34">
                                    <StackPanel Orientation="Horizontal">
                                        <Viewbox Width="13" Height="13" Margin="0,0,6,0"><Path Data="{StaticResource LucideSquare}" Style="{StaticResource LucideIcon}" Stroke="{StaticResource Warning}"/></Viewbox>
                                        <TextBlock Text="Stop"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="14" Style="{StaticResource SoftButton}" Click="ClearGooseSubscriber_Click"
                                        Padding="11,6" MinHeight="34" ToolTip="Clear streams">
                                    <StackPanel Orientation="Horizontal">
                                        <Viewbox Width="13" Height="13" Margin="0,0,6,0"><Path Data="{StaticResource LucideX}" Style="{StaticResource LucideIcon}" Stroke="{StaticResource Danger}"/></Viewbox>
                                        <TextBlock Text="Clear"/>
                                    </StackPanel>
                                </Button>
                            </Grid>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border CornerRadius="9" Padding="8,3" BorderThickness="1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#F2F4F7"/>
                                            <Setter Property="BorderBrush" Value="#D0D5DD"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsGooseCapturing}" Value="True">
                                                    <Setter Property="Background" Value="#E8F8EF"/>
                                                    <Setter Property="BorderBrush" Value="#86D3A7"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding GooseCaptureStateText}" FontSize="9.8" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#667085"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsGooseCapturing}" Value="True"><Setter Property="Foreground" Value="#067647"/></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                                <TextBlock Grid.Column="2" Text="{Binding GooseStatusText}" FontSize="10.7" Foreground="{StaticResource Muted}"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding GooseBindingText}"/>
                                <TextBlock Grid.Column="3" Text="{Binding GooseCounterText}" FontSize="10.7" FontWeight="SemiBold" Foreground="#45648E"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="9">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Streams" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                            <Grid Grid.Row="2">
                                <DataGrid ItemsSource="{Binding GooseStreams}" SelectedItem="{Binding SelectedGooseStream, Mode=TwoWay}"
                                          IsReadOnly="True" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False"
                                          EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                          ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" RowHeight="37">
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                            <Setter Property="ToolTip" Value="{Binding DiagnosticsSummary}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasDiagnostics}" Value="True">
                                                    <Setter Property="Background" Value="#FFFAEB"/>
                                                    <Setter Property="BorderBrush" Value="#FEC84B"/>
                                                    <Setter Property="BorderThickness" Value="3,0,0,1"/>
                                                </DataTrigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="#E7F0FF"/>
                                                    <Setter Property="BorderBrush" Value="#6F9FF1"/>
                                                    <Setter Property="BorderThickness" Value="3,0,0,1"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="IED" Binding="{Binding ModelIedName}" Width="105" MinWidth="85"/>
                                        <DataGridTextColumn Header="APPID" Binding="{Binding AppIdText}" Width="70"/>
                                        <DataGridTextColumn Header="goCBRef" Binding="{Binding GoCbRef}" Width="205" MinWidth="150"/>
                                        <DataGridTextColumn Header="DataSet" Binding="{Binding DataSetReference}" Width="205" MinWidth="150"/>
                                        <DataGridTextColumn Header="Source" Binding="{Binding SourceMac}" Width="118"/>
                                        <DataGridTextColumn Header="Destination" Binding="{Binding DestinationMac}" Width="118"/>
                                        <DataGridTextColumn Header="VLAN" Binding="{Binding VlanText}" Width="96"/>
                                        <DataGridTextColumn Header="stNum" Binding="{Binding StateNumberText}" Width="64"/>
                                        <DataGridTextColumn Header="sqNum" Binding="{Binding SequenceNumberText}" Width="64"/>
                                        <DataGridTextColumn Header="State" Binding="{Binding SequenceStatus}" Width="104"/>
                                        <DataGridTextColumn Header="TAL" Binding="{Binding TimeAllowedToLiveText}" Width="76"/>
                                        <DataGridTextColumn Header="confRev" Binding="{Binding ConfigurationRevisionText}" Width="74"/>
                                        <DataGridTextColumn Header="Flags" Binding="{Binding FlagsText}" Width="90"/>
                                        <DataGridTextColumn Header="Δ" Binding="{Binding ChangedValueCount}" Width="52"/>
                                        <DataGridTextColumn Header="Packets" Binding="{Binding PacketCount}" Width="70"/>
                                        <DataGridTextColumn Header="Model" Binding="{Binding BindingSource}" Width="90"/>
                                        <DataGridTextColumn Header="Seen" Binding="{Binding LastSeenText}" Width="100"/>
                                        <DataGridTextColumn Header="Diagnostics" Binding="{Binding DiagnosticsSummary}" Width="230" MinWidth="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Visibility="{Binding GooseNoStreamsVisibility}" Background="#FAFCFF" BorderBrush="#DCE6F3" BorderThickness="1"
                                        CornerRadius="12" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360">
                                    <StackPanel>
                                        <TextBlock Text="No GOOSE traffic" FontSize="13.2" FontWeight="SemiBold" Foreground="{StaticResource Ink}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Choose an adapter and press Start." FontSize="10.8" Foreground="{StaticResource Muted}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Grid.Row="4" Style="{StaticResource Card}" Padding="9">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="DataSet values" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                    <TextBlock Text="{Binding GooseSelectedStreamText}" FontSize="10.4" Foreground="{StaticResource Muted}" Margin="0,1,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <Border Grid.Column="1" Background="#EEF4FF" BorderBrush="#C9D9F1" BorderThickness="1" CornerRadius="8" Padding="7,3" VerticalAlignment="Center">
                                    <TextBlock Text="allData order" FontSize="9.6" FontWeight="SemiBold" Foreground="#45648E"/>
                                </Border>
                            </Grid>
                            <Grid Grid.Row="2">
                                <DataGrid ItemsSource="{Binding SelectedGooseStream.Leaves}" IsReadOnly="True"
                                          Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False"
                                          EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                          ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" RowHeight="38">
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChanged}" Value="True">
                                                    <Setter Property="Background" Value="#FFF6D8"/>
                                                    <Setter Property="BorderBrush" Value="#F59E0B"/>
                                                    <Setter Property="BorderThickness" Value="3,0,0,1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="#" Binding="{Binding Order}" Width="46"/>
                                        <DataGridTextColumn Header="Signal" Binding="{Binding SignalName}" Width="175" MinWidth="130"/>
                                        <DataGridTextColumn Header="Reference" Binding="{Binding SignalReference}" Width="2*" MinWidth="230"/>
                                        <DataGridTemplateColumn Header="Value" Width="1.15*" MinWidth="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="8" Padding="8,3" HorizontalAlignment="Left" MinWidth="86" BorderThickness="1">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#F2F4F7"/>
                                                                <Setter Property="BorderBrush" Value="#D0D5DD"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsChanged}" Value="True">
                                                                        <Setter Property="Background" Value="#FFF1C7"/>
                                                                        <Setter Property="BorderBrush" Value="#F59E0B"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Value}" Value="&lt;missing in frame&gt;">
                                                                        <Setter Property="Background" Value="#FFF1F2"/>
                                                                        <Setter Property="BorderBrush" Value="#FDA29B"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding Value}" FontWeight="SemiBold" Foreground="#344054" TextTrimming="CharacterEllipsis" ToolTip="{Binding Value}"/>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="FC" Binding="{Binding FunctionalConstraint}" Width="54"/>
                                        <DataGridTextColumn Header="CDC / type" Binding="{Binding TypeText}" Width="120"/>
                                        <DataGridTextColumn Header="Previous" Binding="{Binding PreviousValue}" Width="140"/>
                                        <DataGridTextColumn Header="Model" Binding="{Binding BindingSource}" Width="96"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Visibility="{Binding GooseNoLeafValuesVisibility}" Background="#FAFCFF" BorderBrush="#DCE6F3" BorderThickness="1"
                                        CornerRadius="12" Padding="15" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="330">
                                    <StackPanel>
                                        <TextBlock Text="Select a stream" FontSize="13.2" FontWeight="SemiBold" Foreground="{StaticResource Ink}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Ordered values appear here." FontSize="10.8" Foreground="{StaticResource Muted}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- DIAGNOSTICS -->
            <TabItem Header="Diagnostics">
                <Border Style="{StaticResource Card}" Padding="12">
                    <DockPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Diagnostics &amp; Communication Journal" FontSize="15.5" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
                                <TextBlock Text="Connection, discovery, IED identity, RCB/DataSet setup, report verification, fallback, reconnect, and protocol faults."
                                           FontSize="12.2" Foreground="{StaticResource Muted}" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="CopyDiagnosticButton"
                                        Content="Copy Diagnostic"
                                        Style="{StaticResource PrimaryButton}"
                                        Margin="0,0,8,0"
                                        ToolTip="Copy app, engine, network, IED-session, TCP-probe, and recent journal details for support analysis"
                                        Click="CopyDiagnostics_Click"/>
                                <Button Content="Clear Log" Style="{StaticResource SoftButton}" Click="ClearDiagnostics_Click"/>
                            </StackPanel>
                        </Grid>
                        <DataGrid ItemsSource="{Binding Logs}" IsReadOnly="True" Style="{StaticResource ModernDataGrid}"
                                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.CanContentScroll="True">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                            <Setter Property="BorderBrush" Value="#EF4444"/>
                                            <Setter Property="BorderThickness" Value="2,0,0,0"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="WARN">
                                            <Setter Property="BorderBrush" Value="#F59E0B"/>
                                            <Setter Property="BorderThickness" Value="2,0,0,0"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                            <Setter Property="Background" Value="#FFF1F2"/>
                                            <Setter Property="Foreground" Value="#B42318"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="WARN">
                                            <Setter Property="Background" Value="#FFFAEB"/>
                                            <Setter Property="Foreground" Value="#B54708"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=yyyy-MM-dd HH:mm:ss.fff}" Width="190"/>
                                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="90"/>
                                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="145"/>
                                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Margin="0,10,0,0" Background="#EAF1FF" BorderBrush="#CFE0FF" BorderThickness="1" CornerRadius="14" Padding="11,7">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource Accent}" Margin="0,0,9,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding LastStatusText}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Ink}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding RuntimeSummaryText}" FontSize="11.8" Foreground="{StaticResource Muted}" Margin="12,0,0,0"/>
            </Grid>
        </Border>

    </Grid>
</Window>
